## Mavericks Trackpad 2 v0.5.2

- Renaming your trackpad (in Bluetooth settings) once again writes the
  name onto the device itself, so it follows the trackpad to other Macs.

- The update checker no longer stays open when a check can't reach the
  server; it now closes cleanly instead.
